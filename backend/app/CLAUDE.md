# CLAUDE.md (app)

## Directory Structure
* **models**: contains `sqlalchemy` data model definitions.
* **routers**: contains the API routers for the backend application.
* **schemas**: define API schemas for request/response formatting.
* **services**: are core services supporting all aspects of backend interactions with data / processing.
* **utils**: shared utilities across components to support various common interactions.